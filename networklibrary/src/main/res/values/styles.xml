<resources>
    <!-- loadingdialog样式 -->
    <style name="LoadingDialogStyle">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>


    <!-- loadingdialog显示消失动画-->
    <style name="LoadingDialogAnimStyle">
        <item name="android:windowShowAnimation">@anim/loadingdialog_enter_anim</item>
        <!-- 指定显示的动画xml  -->
        <item name="android:windowHideAnimation">@anim/loadingdialog_exit_anim</item>
        <!-- 指定消失的动画xml  -->
    </style>
</resources>